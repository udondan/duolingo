# Node.js
node_modules/
dist/

# opencode local config (contains credentials)
opencode.json

# Environment variables (may contain secrets)
.env
.env.local

# Python (legacy — kept for reference during migration)
__pycache__/
*.py[cod]
*.pyo
.venv/
.python-version
uv.lock
.ruff_cache/

# Coverage
coverage/
